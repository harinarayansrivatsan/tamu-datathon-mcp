# ========================================
# QUICK START GUIDE
# ========================================
#
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Required for basic functionality:
#    - GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET (for user auth)
#    - GOOGLE_API_KEY (for Gemini AI responses)
#    - SECRET_KEY (generate with: openssl rand -hex 32)
#
# 3. Optional (for full features):
#    - SPOTIFY_CLIENT_ID and SPOTIFY_CLIENT_SECRET (mood detection)
#    - MEETUP_API_KEY and EVENTBRITE_TOKEN (event recommendations)
#
# 4. Frontend also needs configuration:
#    See frontend/.env.example
#
# ========================================

# Loneliness Combat Engine - Environment Variables
# Copy this file to .env and fill in your actual API keys

# App Configuration
APP_NAME=Loneliness Combat Engine
APP_VERSION=1.0.0
ENVIRONMENT=development
DEBUG=true

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_PREFIX=/api/v1

# Database Configuration
DATABASE_URL=sqlite:///./lce.db
DATABASE_ECHO=false

# Security & Authentication
SECRET_KEY=your-secret-key-change-this-in-production-use-openssl-rand-hex-32
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Google OAuth (for user authentication and calendar access)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:8000/api/v1/auth/google/callback

# Google AI / Gemini
GOOGLE_API_KEY=your-google-ai-api-key
GEMINI_MODEL_FLASH=gemini-1.5-flash
GEMINI_MODEL_PRO=gemini-1.5-pro

# Spotify API (for mood detection)
SPOTIFY_CLIENT_ID=your-spotify-client-id
SPOTIFY_CLIENT_SECRET=your-spotify-client-secret
SPOTIFY_REDIRECT_URI=http://localhost:8000/api/v1/auth/spotify/callback

# GitHub API (for late-night coding patterns)
GITHUB_TOKEN=your-github-personal-access-token

# Event APIs
MEETUP_API_KEY=your-meetup-api-key
EVENTBRITE_TOKEN=your-eventbrite-api-token

# Weather API (for seasonal affective patterns)
WEATHER_API_KEY=your-weather-api-key

# Risk Scoring Configuration
BASELINE_PERIOD_DAYS=14
RISK_SCORE_LOW_THRESHOLD=25
RISK_SCORE_MODERATE_THRESHOLD=50
RISK_SCORE_ELEVATED_THRESHOLD=75

# CORS Configuration (comma-separated origins)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000

# Logging
LOG_LEVEL=INFO

# ========================================
# How to get API keys:
# ========================================

# Google AI / Gemini:
# 1. Go to https://makersuite.google.com/app/apikey
# 2. Create a new API key
# 3. Copy the key to GOOGLE_API_KEY

# Google OAuth:
# 1. Go to https://console.cloud.google.com/apis/credentials
# 2. Create OAuth 2.0 Client ID
# 3. Add authorized redirect URIs:
#    - http://localhost:8000/api/v1/auth/google/callback (for sign-in)
#    - http://localhost:8000/api/v1/auth/google/calendar-callback (for calendar)
# 4. Copy Client ID and Client Secret

# Spotify:
# 1. Go to https://developer.spotify.com/dashboard
# 2. Create a new app
# 3. Add redirect URI: http://localhost:8000/api/v1/auth/spotify/callback
# 4. Copy Client ID and Client Secret

# GitHub:
# 1. Go to https://github.com/settings/tokens
# 2. Generate new token (classic) with 'repo' scope
# 3. Copy the token to GITHUB_TOKEN

# Meetup:
# 1. Go to https://secure.meetup.com/meetup_api/key/
# 2. Request an API key
# 3. Copy the key to MEETUP_API_KEY

# Eventbrite:
# 1. Go to https://www.eventbrite.com/platform/api
# 2. Create a new app
# 3. Copy the Private Token to EVENTBRITE_TOKEN

# Weather API (OpenWeatherMap):
# 1. Go to https://openweathermap.org/api
# 2. Sign up for a free account
# 3. Copy the API key to WEATHER_API_KEY
